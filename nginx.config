server {
    listen 8080; # Cloud Run's favorite port
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html; # Essential for React Router!
    }
}